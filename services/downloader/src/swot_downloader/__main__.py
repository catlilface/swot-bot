"""Entry point for the downloader service."""

import asyncio


async def _amain() -> None:
    # Placeholder entry point; adapters land in Этап 3 (task #10).
    print("swot-downloader service — placeholder")


def main() -> None:
    asyncio.run(_amain())


if __name__ == "__main__":
    main()
